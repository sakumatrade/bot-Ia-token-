"""Pump.fun adapter (spec section 22).

There is no official, public Pump.fun API contract documented here and
none is invented: ``PumpFunProvider`` is an interface any real,
legitimate data source can implement later. Until one exists, only
``MockPumpFunProvider`` (explicitly, loudly not-real) is available, so the
rest of the pipeline can be built and tested honestly.
"""
